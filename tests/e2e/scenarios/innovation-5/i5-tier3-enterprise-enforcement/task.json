{
  "_benchmark": "INNOVATION-5-Tier3",
  "_h2ai_validates": "INNOVATION-5 — H2-P vs B3 on 6-constraint task; Tier 3 is the primary hypothesis test",
  "_tenant_id": "innovation-5-team",
  "_timeout_s": 7200,
  "_compare_semantics": "strict",
  "description": "Design the complete budget enforcement subsystem for a multi-tenant SaaS platform. The system must simultaneously satisfy all six of the following non-negotiable requirements: (1) IDEMPOTENCY — if a quota-change or quota-check request is retried within 30 seconds, the system recognizes the duplicate and does not apply the operation twice; (2) IMMUTABLE AUDIT — every quota change is permanently recorded in an append-only log that cannot be modified or deleted; (3) NO DISTRIBUTED LOCKS — quota enforcement never uses distributed locks, advisory locks, or WATCH/MULTI/EXEC optimistic transactions; (4) STRICT TENANT ISOLATION — tenant_id for all operations comes exclusively from the authenticated JWT, never from user-supplied input; (5) ATOMIC QUOTA CHANGE — a quota change that updates multiple quota fields must apply all fields together or none — partial updates must never persist; (6) NO DESTRUCTIVE PRODUCTION OPS — no DELETE, DROP, TRUNCATE, or blind overwrite may be performed on any production quota or audit data, including in migration scripts. Provide the complete write path, failure modes, and how all six constraints are upheld concurrently.",
  "pareto_weights": {
    "diversity": 0.3,
    "containment": 0.6,
    "throughput": 0.1
  },
  "explorers": {
    "count": 3,
    "tau_min": 0.2,
    "tau_max": 0.75,
    "slot_configs": [
      {
        "role_frame": "You are a Redis atomicity and distributed systems specialist who has replaced every distributed lock in a high-traffic billing platform with Lua EVAL scripts. You have debugged double-charge incidents (TOCTOU), split-brain enforcement (WATCH/MULTI/EXEC retry storms), and cross-tenant key collision bugs. Your standard is: every multi-step quota state mutation is a single Lua EVAL; every tenant-scoped key is prefixed with a JWT-derived tenant_id passed as a KEYS argument.",
        "cot_style": "step_by_step",
        "focus_mandate": "The quota enforcement hot path must be a single Lua EVAL that atomically: (1) checks the idempotency key, (2) checks quota balance, (3) decrements (or rejects), (4) stores the idempotency key with TTL. A multi-field quota change must use a Lua EVAL that reads and writes all fields in one script — partial commit is impossible because Lua scripts are atomic. No WATCH/MULTI/EXEC, no SETNX, no GET+SET, no advisory locks anywhere in the path. All Redis key prefixes use tenant_id passed as a safe KEYS parameter, never string-concatenated from user input.",
        "rejection_criteria": "Reject any WATCH/MULTI/EXEC in the quota path. Reject GET+DECR sequences. Reject tenant_id sourced from request body or headers. Reject multi-field quota changes that can partially apply. Reject distributed lock acquisition before quota operations.",
        "constraint_domains": ["idempotency", "atomicity", "concurrency", "multi-tenancy"]
      },
      {
        "role_frame": "You are a compliance and database safety engineer who has managed SOC 2 and ISO 27001 audits. You enforce: all production data changes are non-destructive; billing events flow to an immutable audit pipeline; no migration script may ever DROP or TRUNCATE production data.",
        "cot_style": "backward_chaining",
        "focus_mandate": "Every quota change must publish a billing event to the Kafka financial-events topic after the Redis Lua debit succeeds. Use a local in-memory WAL-backed retry queue on the critical path (queue append takes <0.1ms — no fsync): try kafka.produce(event, timeout_ms=50), on failure append to in-memory queue; background thread drains to Kafka with at-least-once delivery. Billing events flow Kafka → ClickHouse (append-only, no UPDATE or DELETE — this is the immutable audit trail). CockroachDB is updated asynchronously by a Kafka consumer — never on the critical charge path. PostgreSQL 15 is used only for quota configuration and metadata tables. Each audit record must contain: tenant_id (from JWT), actor_id, request_id, operation_type, old_state (JSONB), new_state (JSONB), timestamp_ms. Idempotent retries must emit an audit record with operation_type='deduplicated'. No production quota data may ever be deleted — soft-delete with a 'revoked' flag is the maximum allowed operation. All migration scripts must be additive (ALTER TABLE ADD COLUMN, new tables only).",
        "rejection_criteria": "Reject synchronous Kafka publish with acks=all on the critical charge path (violates ≤0.5ms P99). Reject synchronous fsync or disk writes on the critical path. Reject PostgreSQL as the primary billing event audit store (Kafka → ClickHouse required). Reject quota deletion (must use revocation flags). Reject migrations with DROP TABLE, TRUNCATE, or DELETE without WHERE. Reject audit records that omit old_state or request_id.",
        "constraint_domains": ["audit", "immutability", "safety"]
      },
      {
        "role_frame": "You are an adversarial systems architect who has been hired to find the constraint violation in every proposed design. You look for designs where one constraint appears satisfied but is actually broken in edge cases: a constraint that holds under normal operation but breaks under concurrent retries, partial failures, or schema migrations.",
        "cot_style": "devils_advocate",
        "focus_mandate": "Challenge the design against all six constraints simultaneously. Specifically look for: (1) does the idempotency check cover the multi-field atomic update — can a partial update be committed and then a retry apply the remaining fields? (2) does the audit log capture multi-field atomic changes atomically or can audit write fail after quota write succeeds? (3) does any migration script contain an implicit destructive operation (e.g., recreating a column by DROP + ADD)? (4) does the tenant isolation hold for the audit write, not just the Redis key construction? (5) can a race between two concurrent requests for the same tenant produce a partial quota state despite the Lua EVAL?",
        "rejection_criteria": "Reject designs where any one of the six constraints can be violated in any realistic failure mode: network partition, process restart mid-write, concurrent requests, or schema migration execution.",
        "constraint_domains": ["idempotency", "audit", "atomicity", "multi-tenancy", "safety"]
      }
    ]
  },
  "constraints": [
    "CONSTRAINT-004",
    "CONSTRAINT-005",
    "CONSTRAINT-008",
    "CONSTRAINT-TAU-1",
    "CONSTRAINT-TAU-2",
    "CONSTRAINT-BFT-1"
  ],
  "context": "Infrastructure: Redis 7.2 (quota counters, idempotency keys — TTL ≤30s), Kafka (financial-events topic for billing pipeline), ClickHouse (immutable financial audit — append-only, no UPDATE/DELETE, 7-year retention), CockroachDB (operational reconciliation ledger — updated asynchronously by Kafka consumer), PostgreSQL 15 (quota configuration and metadata tables only). Current broken design: (1) quota decrement uses Redis GET + DECR (TOCTOU race); (2) quota changes for multi-field limits are applied field-by-field in a loop (partial update risk); (3) no immutable billing event pipeline (audit log mutable); (4) tenant_id accepted from X-Tenant-ID header; (5) a past migration script dropped and recreated the quota_limits table to rename a column, wiping 90 days of quota history; (6) a distributed advisory lock (pg_advisory_lock) is held during quota enforcement, causing timeout cascades under load.",
  "_expected": {
    "terminal": "MergeResolved",
    "valid_proposals_min": 1,
    "should_prune": [
      {
        "description": "Proposal that uses WATCH/MULTI/EXEC for quota enforcement",
        "violates": "CONSTRAINT-008 — WATCH/MULTI/EXEC is a distributed lock mechanism; creates retry storms"
      },
      {
        "description": "Proposal that extracts tenant_id from the X-Tenant-ID HTTP header for quota key construction",
        "violates": "CONSTRAINT-TAU-1 — user-supplied tenant_id allows cross-tenant quota manipulation"
      },
      {
        "description": "Proposal that updates multi-field quota in a loop (field-by-field) rather than atomically",
        "violates": "CONSTRAINT-TAU-2 — partial quota state can persist if the process dies mid-loop"
      },
      {
        "description": "Proposal whose migration script uses DROP COLUMN + ADD COLUMN to rename a field",
        "violates": "CONSTRAINT-BFT-1 — DROP COLUMN on production data is destructive and irreversible"
      }
    ],
    "_review_checklist": [
      {
        "id": "I5T3/1",
        "text": "Does the design use a single Lua EVAL that atomically checks the idempotency key, verifies quota balance, and applies the decrement — with no GET/DECR separation and no WATCH/MULTI/EXEC?"
      },
      {
        "id": "I5T3/2",
        "text": "Does a multi-field quota change atomically apply all fields together (e.g., single Lua EVAL with multiple HSET fields, or a single PostgreSQL UPDATE) — making partial update impossible?"
      },
      {
        "id": "I5T3/3",
        "text": "Is the audit log table protected so the application user cannot UPDATE or DELETE audit rows (e.g., row-level security, REVOKE UPDATE, or a separate write-only schema)?"
      },
      {
        "id": "I5T3/4",
        "text": "Is the tenant_id for all Redis key prefixes and audit records derived exclusively from the authenticated JWT — not from the request body, URL path, or headers?"
      },
      {
        "id": "I5T3/5",
        "text": "Does the proposed migration strategy use additive operations only (ALTER TABLE ADD COLUMN, new tables) — no DROP, TRUNCATE, or DELETE without WHERE on production tables?"
      },
      {
        "id": "I5T3/6",
        "text": "Is an audit record written for deduplicated retries (change_type='deduplicated'), ensuring the audit log captures all requests including recognized duplicates?"
      }
    ]
  }
}
