# Benchmark: HLE (Humanity's Last Exam) — expert graduate-level BFT reasoning
# Validates: Byzantine-robust aggregation (Krum + Weiszfeld) across 5 explorers
# Safety profile "production": krum=1 (n≥2f+3=5 required), shadow_auditor enabled + strict=true (AND-vote always binding)
# Note: "strict" (krum=2) requires n≥7 proposals — use separate config with 7 adapter_profiles

nats_url = "nats://host.docker.internal:4222"
verifier_consensus_passes = 1
max_autonomic_retries = 4

calibration_adapter_count = 1
calibration_max_tokens    = 512

[safety]
profile = "production"

[constraint_wiki]
mode = "fs"
corpus_path = "tests/e2e/constraints"

[[adapter_profiles]]
name = "local-1"
[adapter_profiles.kind.CloudGeneric]
endpoint    = "http://host.docker.internal:8080/v1"
provider     = "llama_cpp"
api_key_env = "NONE"

[[adapter_profiles]]
name = "local-2"
[adapter_profiles.kind.CloudGeneric]
endpoint    = "http://host.docker.internal:8080/v1"
provider     = "llama_cpp"
api_key_env = "NONE"

[[adapter_profiles]]
name = "local-3"
[adapter_profiles.kind.CloudGeneric]
endpoint    = "http://host.docker.internal:8080/v1"
provider     = "llama_cpp"
api_key_env = "NONE"

[[adapter_profiles]]
name = "local-4"
[adapter_profiles.kind.CloudGeneric]
endpoint    = "http://host.docker.internal:8080/v1"
provider     = "llama_cpp"
api_key_env = "NONE"

[[adapter_profiles]]
name = "local-5"
[adapter_profiles.kind.CloudGeneric]
endpoint    = "http://host.docker.internal:8080/v1"
provider     = "llama_cpp"
api_key_env = "NONE"

[[adapter_profiles]]
name = "researcher"
[adapter_profiles.kind.CloudGeneric]
endpoint    = "http://host.docker.internal:8080/v1"
provider     = "llama_cpp"
api_key_env = "NONE"

[[adapter_profiles]]
name = "shadow_auditor"
[adapter_profiles.kind.CloudGeneric]
endpoint    = "http://host.docker.internal:8080/v1"
provider     = "llama_cpp"
api_key_env = "NONE"

[hitl]
enabled = false

[thinking_loop]
enabled            = true
max_iterations     = 5
coverage_threshold = 0.80

debug_log_path = "/tmp/h2ai-debug-hle-expert-consensus.ndjson"

[gap_i1]
enabled                  = true
cold_check_threshold     = 0.0
synthesis_min_confidence = 0.7
max_gap_records_per_wave = 3
researcher_timeout_secs  = 300

# ── Complexity-Ceiling Routing ────────────────────────────────────────
# HLE tasks are formal multi-sub-claim proofs (BFT safety + liveness + adversarial
# correctness) — complexity 4 on the probe scale. Route to H1 grafting on first
# ZeroSurvival instead of burning all 4 retries on a structurally intractable task.
[complexity_routing]
enabled                        = true
complexity_probe_adapter       = "researcher"
complexity_probe_timeout_secs  = 30
decompose_threshold            = 4   # score ≥ 4 → graft on first failure
hitl_threshold                 = 5   # score = 5 → HITL immediately (reserved for true intractability)
verifier_decomposition_enabled = false

[complexity_routing.intra_retry]
enabled                       = true
entropy_threshold             = 0.6   # peaked failure distribution → ceiling signal
retry_slope_threshold         = 0.05  # < 5% relative improvement → no convergence
n_eff_cg_product_threshold    = 0.3   # low effective-N × CG-mean → correlated failure
min_retry_count_for_detection = 1     # detect ceiling faster on known-hard HLE tasks
